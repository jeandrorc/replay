# `@replay/desktop`

Reserved for the Tauri + React macOS menu-bar application. This is the
composition root: it may wire all ports and adapters but must contain no
business rules. Scaffold only in story UI-001 after the foundation stories are
complete.

Expected internal areas, created when needed: `src/ui`, `src/ipc`,
`src/composition`, and `src-tauri`.
