# `<ID>` — `<story title>`

- Status: Not started
- Epic: `<link>`
- Depends on: `<IDs or none>`

## User story

As a `<persona>`, I want `<capability>` so that `<outcome>`.

## Context

State the validated problem and link relevant architecture sections/ADRs. Do not
prescribe an implementation unless it is an accepted constraint.

## Acceptance criteria

- [ ] Given `<context>`, when `<action>`, then `<observable result>`.
- [ ] Failure and boundary behavior is explicit.
- [ ] Privacy, offline behavior, and accessibility are addressed where relevant.

## Architectural constraints

- Owning layer/package:
- Ports involved:
- Forbidden dependencies/data:
- ADR required: yes/no and why

## Test obligations

- Unit:
- Contract/integration:
- Manual/e2e:

## Out of scope

- List tempting adjacent work that this story must not implement.

## Implementation notes

Leave empty until implementation. Record facts discovered from the repository,
not guesses.

## Completion evidence

- Commands executed:
- Tests/results:
- Documentation/ADR changes:
- Remaining limitations:
